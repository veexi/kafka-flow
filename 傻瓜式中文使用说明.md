# 📘 KafkaFlow 傻瓜式超详细“人话”中文使用手册 🚀

为了帮助团队中不同技术背景的同学（开发、测试、产品、运营）能够无门槛、秒级上手使用 **KafkaFlow** 可视化平台，本手册将界面上所有晦涩的专业名词翻译成了**生动的比喻**，并按照界面上每一个**按钮和输入框的位置**提供了全方位的汉化对照及场景实操指南。

---

## 🧭 零基础：Kafka 核心名词“人话”大白话比喻

在开始点击按钮之前，我们先用最简单的话来理解 Kafka 的运作方式：
* **Topic (主题) ── 📬“收发件的大箱子”**：比如我们有“订单消息大箱子”、“短信发送大箱子”。
* **Partition (分区) ── 🗄️“箱子里的独立小隔间”**：箱子太大了装不下，于是被隔成了几个小隔间（隔间 0, 隔间 1...）。
* **Offset (偏移量) ── 🔖“信件在隔间里的排队编号”**：每个小隔间里的信件都有一个从 0 开始递增的编号。数字越小，说明信件越老；数字越大，说明信件越新。
* **Payload (载荷/值) ── 📄“信件的具体文字内容”**：也就是发过来的那段 JSON 数据或文本正文。
* **Consume (消费) ── 📥“伸手从箱子里拿信来看”**：也就是抓取、读取数据的意思。
* **Produce (生产) ── 📤“写一封信塞进箱子里”**：也就是发送测试数据的意思。
* **Lag (积压量) ── ⏳“来不及看、堆积在那里的信件数量”**：如果发件人发了 100 封信，你只看了 80 封，那么还有 20 封没看。这 20 就是 Lag。积压量越大，说明你的系统消费能力出问题了，也就是俗称的“消息卡住了/延迟了”。

---

## 🎛️ 第一部分：界面按钮与模块“手把手”汉化对照

### 1. 左侧控制栏与主题列表 (Left Sidebar)
* **`Configure` (配置中心按钮)** 👈 左上角那个灰色/蓝色的齿轮按钮。点击它可以新建或修改 Kafka 连接（比如添加新集群的 IP 地址）。
* **`Refresh` (刷新按钮)** 🔄 用来刷新当前连接的 Kafka，看看有没有新创建的“箱子”（Topic）。
* **`Search topics...` (搜索框)** 🔍 快速过滤左侧的 Topic。输入几个字母，就能立刻找到你要的 Topic。

---

### 2. 顶部状态栏 (Header Info)
* **`Total messages across partitions: 12,444`** 📊 告诉你当前选中的这个 Topic 里面**一共堆积了 12,444 条消息**。

---

### 3. 数据浏览页签 (Browse Messages Tab)
这是最常用的板块，点击 Topic 后，在上方选择 **`Browse Messages`** 即可进入。

#### 🎛️ 控制栏参数翻译：
* **`PARTITION` (选择隔间)**：
  * 默认 `All Partitions`（看所有隔间里的信）。也可以指定只看 `Partition 0` 或 `Partition 1`。
* **`SEEK TYPE` (从哪开始看起)**：
  * **`Newest / End` (最新/结尾)** ⭐【最常用】：只看**刚刚和最新发进来**的信，以前的老信不看。
  * **`Oldest / Start` (最老/开头)**：从**盘古开天辟地第一条**消息开始看。
  * **`Specific Offset` (指定排队号)**：指定看第几封信（需输入具体的编号数字）。
  * **`Date / Timestamp` (指定时间点)**：选择一个日期时间（比如今天下午3点），只看**3点以后**发进来的信。
* **`LIMIT` (一次拿多少封)**：
  * 默认 `100 messages`。也就是一次最多只拉取 100 条消息展示，防止消息太多卡死浏览器。
* **`KEY DECODER` / `VALUE DECODER` (解码器)**：
  * 默认 `Auto Detect`（自动识别识别）。如果是乱码，可以手动切换成 `Plain Text (纯文本)`。如果是高级的 Avro 数据，切换为 `Avro Deserializer`。
* **`SORT BY` (排序方式)**：
  * `Time (Newest First)` ⭐【推荐】：最新的消息排在最上面。
* **`SERVER SEARCH KEYWORD` (服务器深度搜索词)**：
  * 🔍 如果你想从几万条历史消息中**找特定内容**，在此输入词，然后点击 `Consume`。后端会自动帮你把每一封信拆开匹配。
* **`Consume` (蓝色大按钮 - “抓取消息”)** ⭐：
  * **点它！** 只要你改了上面的任何参数，都必须点一下这个蓝色的大按钮，才会重新去 Kafka 抓取最新数据。

---

### 4. 消息列表网格 (Message List Grid)
抓取成功后，左下角会显示一个网格列表：
* **`PARTITION / OFFSET / TIME` (左列: 消息位置与时间)**：
  * 展示 `P1 #711 10:45:59`。意思是：**分区 1** 里的 **第 711 封信**，是在 **10:45:59 收到**的。
* **`Filter displayed messages...` (中间输入框 - 本地快速过滤)** ⭐：
  * 💡 **非常好用！** 在这里输入任意词，列表里已经加载出来的消息会**秒级筛选**，不符合条件的行会立刻隐藏。
* **`PAYLOAD PREVIEW` (右列: 内容预览)**：
  * 展示这一行消息的部分正文，超出部分会自动变成 `...`，非常方便一眼扫视。

---

### 5. 消息详情面板 (Selected Event Attributes - 右侧区域)
点击左侧列表的任意一条消息，右侧会展示该消息的**绝对完整内容**：
* **`RECORD KEY` (事件钥匙)**：有些消息有 Key，有些为 `/* [No Key] */`（无Key）。
* **`RECORD PAYLOAD VALUE` (事件正文内容)** ⭐：
  * 展示该消息的完整 JSON 或文本。
  * 💡 **小技巧**：点击黑框右上角蓝色的 **`FORMAT JSON`** 按钮，一秒把挤成一坨的文本排版得整整齐齐；点击 **`RAW`** 可以切换回原始状态。

---

## 🛠️ 第二部分：傻瓜式常用场景操作步骤 (Step-by-Step)

### 场景 1：测试发了新消息，我想看“刚刚发出来的最新消息”
1. 左侧列表里，鼠标左键点击你的 **Topic 名称**。
2. 顶部选择 **`Browse Messages`** 页签。
3. **`PARTITION`** 选 `All Partitions`。
4. **`SEEK TYPE`** 选 **`Newest / End`**（最新/结尾）。
5. **`LIMIT`** 选 `100 messages`。
6. 点击蓝色大按钮 **`Consume`**。
7. 在下方消息列表中，排在第一条的就是最新的那条消息！点击它，在右侧看正文。

---

### 场景 2：消息太多了，我想快速找到含有“订单号: 123456”的那条消息
* **方法 A：如果消息刚发不久（快速本地找）：**
  1. 按照 **场景 1** 的步骤，把最新的 100 条消息抓取下来。
  2. 在列表头部的 **`Filter displayed messages...`** 框中，直接输入 `123456`。
  3. 列表中会瞬间过滤只剩下包含该订单号的行，点击查看即可。
* **方法 B：如果消息是很久以前发的（服务器深度找）：**
  1. 在上方控制栏的 **`SERVER SEARCH KEYWORD`** 输入框中，输入 `123456`。
  2. 点击蓝色大按钮 **`Consume`**。
  3. 后台会自动深入扫描最近的 30,000 条历史记录，并将带有 `123456` 的消息精准呈现在列表里！

---

### 场景 3：我想自己发一条测试消息到 Kafka 里
1. 顶部选择 **`Produce Message`** (发送消息) 页签。
2. **`PARTITION`** 选 `Auto Partition`（让系统自动分发），或者手动指定 `Partition 0`。
3. **`RECORD KEY`**：可以不填，或者随便填个数字/字串。
4. **`RECORD PAYLOAD VALUE`**：输入你要测试的文本或 JSON。例如：
   ```json
   { "id": 999, "name": "张三", "status": "测试成功" }
   ```
5. **`CUSTOM HEADERS`** (头信息，一般测试不用管，留空即可)。
6. 点击下方绿色/蓝色的 **`Send Message`** 按钮。
7. 提示成功后，切回 **`Browse Messages`** 页面，重新点一下 **`Consume`** 就能立刻看到刚才自己发出的这条测试消息了！

---

### 场景 4：业务系统好像卡住了，怎么看是不是 Kafka 消息堆积了？
1. 顶部选择 **`Consumer Groups`** (消费者组) 页签。
2. 在列表里找到你们业务系统对应的 Group ID（消费组名称）。
3. 观察 **`LAG` (积压量)** 这一列的数字：
   * **如果是 0 或者很小（如个位数）**：说明业务系统看信速度很快，没有卡顿，是别的地方出问题了。
   * **如果是一个很大的数字（如几百几千）**：说明信件疯狂发进来，但系统来不及看，Lag 堆积起来了！赶紧通知开发去查业务系统的消费线程是不是卡死了。

---

### 场景 5：测试环境 Topic 里有很多之前的脏数据，我想把它清空
1. 选中左侧的 **Topic 名称**。
2. 观察 Topic 名字旁边，有几个颜色鲜艳的小按钮。
3. 点击红色的 **`Purge Data`** (清理数据) 按钮。
4. 系统会弹出确认提示，点击确认。
5. 整个 Topic 瞬间被清空，历史脏数据被彻底推走，可以重新干净地开展测试了！

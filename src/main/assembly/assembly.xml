<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 https://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>release</id>
    <formats>
        <format>tar.gz</format>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    
    <fileSets>
        <!-- 1. Pack script folder to script/ and convert CRLF to LF, and grant +x permission -->
        <fileSet>
            <directory>src/main/script</directory>
            <outputDirectory>script</outputDirectory>
            <fileMode>0755</fileMode>
            <lineEnding>unix</lineEnding>
        </fileSet>
        
        <!-- 2. Pack production configurations (including application.yml and config.json) -->
        <fileSet>
            <directory>src/main/config</directory>
            <outputDirectory>config</outputDirectory>
            <lineEnding>unix</lineEnding>
        </fileSet>
        
        <!-- 3. Create empty logs directory -->
        <fileSet>
            <directory>src/main/resources</directory>
            <outputDirectory>logs</outputDirectory>
            <excludes>
                <exclude>**/*</exclude>
            </excludes>
        </fileSet>
    </fileSets>

    <dependencySets>
        <!-- 4. Pack the built fat JAR into bin/ -->
        <dependencySet>
            <outputDirectory>bin</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <includes>
                <include>${project.groupId}:${project.artifactId}</include>
            </includes>
        </dependencySet>
    </dependencySets>
</assembly>
